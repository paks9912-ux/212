# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/
.swiftpm/

# macOS
.DS_Store

# Secrets — the API key belongs in the scheme's environment or the Keychain, never here.
*.env
Secrets.xcconfig
